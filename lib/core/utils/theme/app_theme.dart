import 'package:flutter/material.dart';

class AppColors {
  static const Color whiteColor = Color(0xFFFAFDFF);
  static const Color kPrimaryBg = Color(0xFFE6F2FF);
  static Color blue700 = Colors.blue.shade700;
  static Color blue900 = Colors.blue.shade900;
  static Color blueColor = Colors.blue;
  static Color greenColor = Colors.green;

  static Color greyColor = Colors.grey;
  static LinearGradient greenLinearGradient = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Color.fromARGB(255, 203, 252, 206), Colors.white],
  );
  static Color blackColor = Colors.black;

  static Color darkBlackColor = Color(0xFF0B1426);

}
